/**
 * @param {number} x-is a parameter.
 *  @param {number} y-is a parameter.
 * @return finally using tdd testing we have to test the distance.
 */
let dist=(x,y) =>{
    Math.sqrt(x * x + y * y)
    {
}
}
module.exports=(x,y)=>{
    dist;
};